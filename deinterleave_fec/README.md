# De-interleaving + FEC Decoding — Owner: Tirth

Two-stage module:
1. De-interleaving: Block / Convolutional / Diagonal / Pseudo-Random
2. FEC decoding: Viterbi (convolutional codes), Reed-Solomon, concatenated
   codes, LDPC

See `docs/interface_contract.md` (Steps 4 & 5) for exact input/output format.
