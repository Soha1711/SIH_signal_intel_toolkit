# Demodulation — Owner: Honey

Takes parameter-estimation output and recovers the bit/symbol stream for the
detected modulation scheme (FSK / PSK / QAM). Also generates constellation
plot data for visual verification.

See `docs/interface_contract.md` (Step 3) for exact input/output format.
