# Sample Data

Test `.iq` / `.wav` files with known modulation/FEC parameters, used to
validate each module. Add a short note here for each file: modulation type,
sample rate, and expected output, so everyone can test against ground truth.
